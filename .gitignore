# Machine-local agent settings
.claude/settings.local.json

# Generated notification feed. Contains personal mail metadata — subjects,
# senders, and preview text that can include one-time codes and receipts.
# Regenerated by notifications-build.js; never commit it.
notifications.json
notifications.json.tmp
notifications-batch.json
