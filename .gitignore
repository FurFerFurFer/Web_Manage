# Machine-local agent settings
.claude/settings.local.json

# Leftover feed from the removed Notifications page. Contains personal mail
# metadata — subjects, senders, and preview text that can include one-time codes
# and receipts. Nothing generates or reads it any more: delete the local file,
# then delete this rule.
notifications.json
